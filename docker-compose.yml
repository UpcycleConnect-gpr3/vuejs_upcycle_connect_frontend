services:
  upcycle_front_dev:
    build:
      context: .
      target: dev
    container_name: upcycle_front_dev
    volumes:
      - ./src:/app/src
    networks:
      - traefik_network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.upcycle_front_dev.rule=Host(`upcycle-front.localhost`)"
      - "traefik.http.services.upcycle_front_dev.loadbalancer.server.port=5173"
    profiles:
      - dev

  upcycle_front_prod:
    build:
      context: .
      target: prod
    container_name: upcycle_front_prod
    networks:
      - traefik_network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.upcycle_front_prod.rule=Host(`upcycle-front.localhost`)"
      - "traefik.http.services.upcycle_front_prod.loadbalancer.server.port=80"
    profiles:
      - prod

networks:
  traefik_network:
    external: true
